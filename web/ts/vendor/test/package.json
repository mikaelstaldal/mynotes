{
  "name": "mynotes-vendor-rebuild-test",
  "private": true,
  "type": "module",
  "description": "Throwaway manifest for web/ts/vendor/rebuild.sh's test-only jsdom bundle — not used at runtime.",
  "dependencies": {
    "jsdom": "29.1.1"
  }
}
